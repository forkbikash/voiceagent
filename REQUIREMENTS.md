We have to create a livekit agent with following requirements:
We should be able to interact in the browser call provided by livekit, so you need to handle speech to text, text to speech(use elevenlabs).
We should be able to use multiple llms parallely for the agent and we continue with the response from llm who responds the fastest with least latency.
We should have end call tool.
Always use livekit framework for the implementation.
Livekit is self hosted.
